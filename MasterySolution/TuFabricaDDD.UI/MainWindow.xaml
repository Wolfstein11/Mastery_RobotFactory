<Window x:Class="TuFabricaDDD.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TuFabricaDDD.UI"
        mc:Ignorable="d"
        Title="Robot Manager" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="RobotsDataGrid" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Serial Number" Binding="{Binding SerialNumber}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTextColumn Header="Location" Binding="{Binding LocationDisplay}" Width="*"/>
                <DataGridTextColumn Header="Network" Binding="{Binding NetworkDisplay}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CreateButton" Content="Create" Width="80" Margin="0,0,10,0" Click="CreateButton_Click" Background="#FFB6C1" Foreground="Black" FontWeight="Bold"/>
            <Button x:Name="UpdateButton" Content="Update" Width="80" Margin="0,0,10,0" Click="UpdateButton_Click" Background="#87CEFA" Foreground="Black" FontWeight="Bold"/>
            <Button x:Name="DeleteButton" Content="Delete" Width="80" Click="DeleteButton_Click" Background="#FFD700" Foreground="Black" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
